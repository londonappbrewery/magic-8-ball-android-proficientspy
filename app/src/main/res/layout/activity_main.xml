<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/background_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/background_image_description"
        android:scaleType="centerCrop"
        app:srcCompat="@drawable/purple_nebula"/>

    <Button
        android:id="@+id/askme_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="38dp"
        android:width="250dp"
        android:background="@android:color/holo_green_dark"
        android:text="@string/askme_button"
        android:textColor="@android:color/white"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="45dp"
        android:background="#FF6E40"
        android:gravity="center"
        android:text="@string/top_heading"
        android:textAllCaps="true"
        android:textColor="@android:color/white"
        android:textSize="24sp"/>

    <ImageView
        android:id="@+id/wheel_of_fortune"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_alignLeft="@+id/askme_button"
        android:layout_alignStart="@+id/askme_button"
        android:layout_centerVertical="true"
        android:contentDescription="@string/fortune_wheel_description"
        app:srcCompat="@drawable/ball1"/>
</RelativeLayout>
